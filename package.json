{
  "name": "cozyreq",
  "private": true,
  "workspaces": ["apps/web"],
  "scripts": {
    "test": "bun run --cwd apps/web lint",
    "lint": "bun run --cwd apps/web lint",
    "format": "bun run --cwd apps/web format",
    "format:check": "bun run --cwd apps/web format:check || echo 'No format:check script defined in apps/web'",
    "type-check": "bun run --cwd apps/web type-check"
  },
  "devDependencies": {
    "oxlint": "latest",
    "oxfmt": "latest",
    "typescript": "^5.0.0"
  }
}
