[tools]
node = "lts"
uv = "latest"

[settings]
python.uv_venv_auto = true

[env]
_.file = '.env'

[tasks."dev:app"]
description = "Start the application in development mode"
run = "npm run tauri dev"
